const anim = { duration: 0.5, ease: [0.74, -0.01, 0.29, 0.99] }
const animOne = { duration: 1, ease: [0.74, -0.01, 0.29, 0.99] }

const transitionDelay = 4000

export { anim, animOne, transitionDelay }
